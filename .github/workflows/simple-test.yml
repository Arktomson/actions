name: Test External Reference
on:
  workflow_dispatch:  # 手动触发

jobs:
  test-external-reference:
    runs-on: ubuntu-latest
    name: Test Print Numbers External Reference
    steps:
      - name: Test print-numbers via external reference
        uses: Arktomson/actions/print-numbers@v1.0.0
        with:
          number: 7
          prefix: "External Test: "
          format: json